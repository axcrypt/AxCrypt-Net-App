@* LEGACY — DO NOT USE
   Legacy inline-styled free-tier banner (pink background, old CSS classes).
No longer mounted in MainLayout — callers use PaidFeaturegateService.ShowPaidGate() instead.
Safe to delete once all references are confirmed gone.
*@
﻿@using AxCrypt.App.Shared.ViewModels
@using AxCrypt.Content

@inject UpgradeSubscriptionViewModel upgradeModel


<div class="popup-section paid-plan-required-popup" id="paid-plan-required-popup">
    <div class="" id="upgrade-sec" style="background-color:#FFCBC9; padding:10px; text-align:center; border-radius:5px;">
        <span style="color: #CE3733;">
            @((MarkupString)(string.Format(Texts.UpgradeFeatureText)))
        </span>
        <button class="upgrd-btn font-medium btn-medium" @onclick="upgradeModel.ShowUpgradeDialog" style="margin: 0 auto;">@Texts.UpgradeText</button>
    </div>
</div>
