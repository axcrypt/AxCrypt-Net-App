// Moved to AxCrypt.App.Entitlement.Contracts.FeatureKey — see that file
// for the canonical definition. This file is a placeholder kept only so
// the project tree stays stable; AxCrypt.App.Shared cannot host the
// type because AxCrypt.App.Shared → AxCrypt.App.Entitlement is the only
// allowed project-reference direction.
namespace AxCrypt.App.Shared.Entitlement;

internal static class _RetiredFeatureKey { }
