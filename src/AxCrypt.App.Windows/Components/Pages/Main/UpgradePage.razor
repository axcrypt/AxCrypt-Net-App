@page "/upgradePage"
@* Legacy route — kept so any deep-link or bookmark still works *@
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/planAndPay", replace: true);
    }
}