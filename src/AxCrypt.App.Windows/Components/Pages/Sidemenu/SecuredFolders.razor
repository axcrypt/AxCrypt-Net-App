@page "/securedfolders"

@using AxCrypt.Api.Model;
@using AxCrypt.App.Components.Models
@using AxCrypt.Core.IO
@using AxCrypt.Core.UI
@using System.Collections.ObjectModel
@using static AxCrypt.Abstractions.TypeResolve

<link href="css/login.min.css" rel="stylesheet" />
<div class="home-index">
    <div>
        <div class="page-title">
            <h1 class="">Secured folders</h1>
        </div>
    </div>
    <AxCrypt.App.Windows.Components.Pages.Main.HomeBody />
    <div>
        <div style="padding: 10px 0px;">
            <label class="text-style font-bold">Recent Folders</label>
        </div>
        <div>
            <RecentFolders />
        </div>
    </div>
</div>
@code {
    SubscriptionLevel SubscriptionLevel { get; set; }

    protected override async Task OnInitializedAsync()
    {
        SubscriptionLevel = New<AccountStatusViewModel>().SubscriptionLevel;

        await base.OnInitializedAsync();
    }
}